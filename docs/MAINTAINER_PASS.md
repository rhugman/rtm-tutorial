# Maintainer pass — prioritized punch list

Distilled from the authoring run's per-notebook reviews and cross-notebook consistency review (2026-06-05). Work top-down; the P0 items gate everything below them.

## P0 — design decision required (author)

1. **RESOLVED & PROPAGATED 2026-06-05 — decision framing is now minimize/design (CONTEXT.md, ADR-0001 updated; all 14 notebooks swept, verified canon-consistent).** ~~Propagation pending~~ Done: payoff beats are distribution-based everywhere, the 90 mg/L lens lives in part0_03/1_03/1_04/1_05 (paired 0.15→0.67 in 1_06), EU 250 reduced to its one sentence per notebook, truth criterion = nearest the prior 87.5th percentile (`TRUTH_PCTILE=0.875`, identical rule in 1_03 and 1_05; picks realisation 101 @ ~91 mg/L on the legacy ensemble), and the forecast group in part1_02 now spans **all three supply screens** (was ly3-only — load-bearing, since the thinned prebaked ensemble keeps only the forecast group). Residual detail for context: Computed from the on-disk ensembles (peak supply-well SO₄, max over screens, mg/L = mol/L × 96.06×10³): prior median 82, 5–95% 72–94 (195 reals); posterior (master_hm iter 3) median 92, 83–97 (187 reals) — conditioning shifts the forecast *up* and tightens it ("truer news, not better news"). Canon: payoff = forecast distribution (median + P95) prior vs posterior; one-cell illustrative risk lens at **90 mg/L** (prior P=0.15 → posterior P=0.67); EU 250 mg/L = one sentence (comfortably met; cost, not compliance, drives the decision); truth criterion = upper prior quartile (peak ≈ 87–95 mg/L); forecast = max over all supply screens. **Propagate to:** part1_03 (`SO4_THRESHOLD`/truth criterion → quartile pick + lens cell), part1_04 (prior payoff = distribution + lens; drop threshold-straddle prose), part1_05 (posterior payoff — resolves its "degenerate 250" blocker; switch from `welopt-ly3`-only to max-over-screens), part1_06 (mg/L convention + distribution comparison), part1_07 (already threshold-free — uncertainty-reduction payoff stands), part1_08 (objective pair unchanged; 90 mg/L line illustrative only), part0_01/03 prose (part0_03's invented P(>250)=0.24 toy → recast with the real numbers or the 90 lens). No prior regeneration needed.
2. **Pyrite rate parameterisation route** — facts established 2026-06-05: `phinp.dat` is *read, not regenerated* by the `mf6rtm` CLI (console script `mf6rtm:run_cmd` → `read_phinp`/`RunFile`), so it is **directly PstFrom-templatable**. The rate law (datab.dat `Pyrite_trans`) is `rate = -10.19 + parm(1) + parm(3)·lm(O2) + parm(4)·lm(H+) + parm(2)·log10(m/m0)` — `parm(1)` (currently 16.0, log₁₀(A/V)) is **additive in log₁₀ rate**, i.e. a perfect single multiplier knob: shifting it by δ multiplies the rate by 10^δ. There are 12 KINETICS zone blocks, all with identical Pyrite `-parms`. **Recommended route:** one global parameter δ on `parm(1)` via a hand-written tpl over `phinp.dat` (12 substitutions), prior δ ∈ [−1, +1] (rate ×0.1…×10). **Sequencing caveat (added 2026-06-06):** the prior MC was run *without* this parameter, so adding it to the template invalidates `prebaked/`, the truth pick, and the baked part1_03/04/05 — it forces a full prior-MC regeneration (≈201 × 7 min; ~2.5 h with 10 workers) plus the downstream re-bakes. Decide whether the rate parameter earns that bill **before** the fresh full-model IES run (item 11a), so both expensive runs happen once, against the same prior.
3. ~~`ies_multimodal_alpha=0.99` rationale~~ RESOLVED 2026-06-06: canon reversed by controlled experiment — with 0.99 the emulator-space posterior over-tightened and excluded the truth (76, 68–83); without it the posterior matched the kNN reference (78, 68–87 vs ref 80, 72–91). **Multimodal dropped everywhere** (author decision), including the future full-model re-run; part1_05 carries the evidence beat.

## P1 — execution pass (in dependency order)

4. ~~`conda env update`~~ DONE 2026-06-05: `modflowapi` + `psutil` installed into `rtm_gmdsi`; full import chain verified (mf6rtm + vendored pyemu/flopy + DSI).
5. ~~**part0_02 pyrite column**~~ DONE 2026-06-05: executed end-to-end, zero errors, outputs baked. The postfix `EQUI("Orgmatter")` risk did not materialise. Better than feared: the **cold run shows O₂ breakthrough at ~day 25** (slower kinetics → oxidant penetrates the column) — the Prommer & Stuyfzand mechanism in miniature; SO₄ plateau ≈ 1e-3 mol/L ≈ 96 mg/L, on the DIZON scale. Optional polish: the warm-run O₂ panel is flat (consumed upstream) — fine as a teaching point now that the cold contrast carries the beat.
6. ~~**part1_01**~~ DONE 2026-06-05: executed end-to-end (reactive run 7.05 min — the ~6 min canon holds), zero errors, outputs baked. Two bugs found+fixed during the pass: (a) the cell-27 workspace reset wiped the grid shapefile before the reactive build re-read it (grid now re-written after the reset); (b) the measured-vs-modelled comparison existed only as an ipywidgets dropdown, which bakes nothing — static SO₄/Tmp panels added (the widget kept for live use). The baked comparison shows the base model tracking the measured SO₄ rise at wp1-f3 and the seasonal Tmp signal — the credibility beat is real. Residual: `data_d` threading through helpers stands as optional polish.
7. **part1_02** — template build DONE 2026-06-05; **dispersivity mystery solved** (REDESIGN open item 2): the resurrected block used `pargp=base.split(".")[0]`, lumping all 12 layers' pilot points (identical plan coordinates) into one covariance block → singular matrix → `build_prior` crash. Almost certainly why it was originally commented out. Fixed to per-layer `pargp=base` (matching K/porosity/pyrite); template now builds cleanly: 15,024 adjustable pars (48 per-layer groups), 201-real prior drawn, forecast group = 1,263 supply-well SO₄ obs across all three screens. Two relocation bugs also fixed during execution (`add_py_function("../herebedragons.py")`). Still open: the single pestpp-ies forward run (in progress), then uncomment the test-run beats + drop their VERIFY admonitions; `noptmax=-2` verification; pyrite-rate tpl (route in P0-2).
8. ~~**part1_03 → prebaked**~~ DONE 2026-06-05: part1_03 executed (truth = realisation 101 @ 91 mg/L, 87.5th pctile); `etc/make_prebaked.py prior-mc` generated `prebaked/prior_mc_obs_ensemble.jcb` (194 reals + base × 27,454 curated obs, 88 MB) + thinned `pest.pst` + provenance sidecar. **One bug found+fixed in the script**: a stale `FORECAST_SCREEN="welopt-ly3"` constant collapsed the forecast group to one screen during thinning (shifting the prior median 82→78) — it now prefers the staged `forecast` obs group verbatim (all three screens; fallback also fixed). part1_04 re-baked against the fixed artifact reproduces canon exactly (median 82, P95 94, P(>90)=0.15). Realisation accounting clarified: 195 rows = 194 realisations + `base` (the mean vector); notebooks exclude `base` from summaries. Redundant 55 MB CSV companion dropped (jcb is canonical).
9. ~~**part1_05**~~ DONE 2026-06-06: executed end-to-end (runstor verified live — REDESIGN open item 3 closed), posterior 78 mg/L (5–95% 68–87) vs truth 90.8 at its ~97th pctile, **calibration audit added: 5-95 band covered 5 of 6 held-out truths**. The conditioning failure-and-repair arc found and fixed: (a) unstable lstsq `encode()` (clip-to-training-range + pinv — was producing ±1e16 mol/L); (b) Tmp σ in wrong units (0.5 °C vs ×1e-3 stored); (c) emulator error measured from the fidelity check and priced into σ_total; (d) within-series correlated noise + matching √n weight deflation; (e) `ies_phi_factor_file` dropped (it silently overrode the noise budget); (f) `energy_threshold` 0.999→0.975 (169 comps ≈ full rank for 194 reals = noise-fitting); (g) multimodal dropped per P0-3. Residual: a public `encode` in pyemu would be nicer than the in-notebook helper; propagate (b)+(c)+(d) to part1_03's canonical noise table (item 11b).
10. **Cross-notebook reconciliations** (from the consistency review): ~~forecast screen-set~~ DONE 2026-06-05 (max-over-screens everywhere: 1_02 forecast group, 1_03/04/05 computations, 1_07/08 stubs all use ly1/ly3/ly5); ~~truth handshake~~ DONE (identical 87.5th-percentile rule in 1_03 and 1_05). Still open: noise table (part1_03: 7%/per-species floors vs part1_05: 5%/single floor — part1_03 is canon, fix 05); verify the `obgnme=="conc"` vs `oname=="conc"` selector fix landed in part1_05. New: confirm the prebaked thinned ensemble retains **all three** welopt screens *and* the wp1-f3/wp4-f5 diagnostic series part1_04's plots expect (its code degrades gracefully but the panels would go placeholder).
11. **Full autotest pass**: `python autotest/nb_tests.py --fast` (part0) first, then full. Every notebook is untested-by-design until this pass.

11a. **NEW (2026-06-06) — `master_hm` is conditioned on the wrong truth.** The legacy full-model IES was run (in xx_dsi days) against the *old* `random_state=0` truth with the old weights — not against r101 and the canonical noise model. part1_06's "validate DSI vs full model" comparison is apples-to-oranges until a fresh full-model IES is run against the r101 truth + canonical weights (≈201 reals × 4 iters; ~9 h with 15 workers, as the original was). The canonical *posterior* numbers (median 92 / 83–97 / lens 0.67) belong to the old setup and are provisional until then — CONTEXT.md's "truer news" example should be re-verified afterwards.
11b. **NEW (2026-06-06) — noise-model lessons from the part1_05 conditioning failure** (must propagate to part1_03, the canonical table): (a) Tmp σ must be in *stored* units (0.5 °C × 1e-3 — the scaling convention strikes again); (b) an explicit **total-error inflation** (×3, judgment call) is required — measurement-only sigmas let IES treat ~1,800 redundant obs as independent info and collapse the posterior so hard it *excluded the truth* (kNN reference: histories matching r101 still carry peaks 72–91 mg/L); (c) noise is drawn correlated within site:species series, per-obs sigma. part1_03's noise cells need the same three changes + re-stage.

## P2 — later (gated on the above)

12. Implement `etc/make_prebaked.py` stubs: `full-model-ies` (thin master_hm) and `dsivc-sweep` (needs part1_08's dvar naming settled: `dv_rate_mult`, `dv_switch_day` assumed). Decide the peak-SO₄ scalarisation for MOU (worst-case P95 across forecast obs vs a derived max-obs baked into the sweep) and how the volume objective is carried (derived obs vs negated less_than group).
13. `bin/linux` binaries (mf6, pestpp-ies, pestpp-mou, libmf6).
14. Record upstream SHAs for flopy/mf6rtm/vorflow in `dependencies/PROVENANCE.md` on their next refresh.
15. part1_07: confirm held-back species framing (model simulates only `ca`, `na`, `fe`, `fe2`; `mg`/`k` are in the conceptual canon but not simulated — prose keeps the concept, code filters to present species). part1_09: replace the stubbed `hbd.assign_chem_noise()` (doesn't exist) with part1_03's actual noise machinery once settled.
16. Units note for CONTEXT.md once confirmed: SO₄ stored in mol/L (×96.06 g/mol → mg/L); Tmp stored ×1e-3 (salvage convention) — pin both after the part1_01 test run.
