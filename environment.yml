name: rtm_gmdsi
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pandas>=2.0
  - numpy>=1.25 
  - matplotlib 
  - jupyter 
  - notebook
  - geopandas
  - rtree
  - pyproj>=2.0
  - pyshp
  - scikit-learn
  - shapely
  - openpyxl
  - psutil
  - pip:
      - -e ./dependencies/flopy
      - -e ./dependencies/pyemu
      - -e ./dependencies/mf6rtm
      - -e ./dependencies/vorflow
      - -e ./dependencies/condor_deploy   # HTCondor deployment helper for FOM pestpp runs (pure stdlib)
      - modflowapi
      - pypestutils
      - vtk